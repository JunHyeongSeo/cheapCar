<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>

	<comment>
		Cs Mapper
	</comment>
	
	<entry key="selectCsListCount">
		SELECT COUNT(*) FROM TB_CS_BOARD
	</entry>
	
	<entry key="selectCsList"> 
		SELECT 
			  *
		  FROM
				(SELECT 
					   A.* , 
				       ROWNUM RNUM 
				  FROM 
				      (SELECT 
				             * 
				        FROM
				            TB_CS_BOARD
                        JOIN
                            TB_MEMBER USING(MEMBER_NO)
				    ORDER BY 
				            CS_NO DESC) A)
    	 WHERE 
				RNUM BETWEEN ? AND ?
	</entry>
	
	<entry key="csDetail">
		SELECT * FROM TB_CS_BOARD JOIN TB_MEMBER USING(MEMBER_NO) WHERE CS_NO = ?
	</entry>
	
	<entry key="insertCs">
		
		INSERT INTO TB_REPLY_BOARD VALUES(SEQ_REPLY_BOARD_NO.NEXTVAL, '답변 대기중', DEFAULT, DEFAULT)
		
		INSERT INTO TB_CS_BOARD VALUES(SEQ_CS_BOARD_NO.NEXTVAL, ?, ?, DEFAULT, DEFAULT, DEFAULT, ?, SEQ_REPLY_BOARD_NO.CURRVAL)
		
		
	</entry>
	
	<entry key="insertAttachment">
		INSERT 
		  INTO 
			  TB_CS_PHOTO(FILE_NO, 
			  			 REF_BNO, 
			  			 ORIGIN_NAME, 
			  			 CHANGE_NAME, 
			  			 FILE_PATH) 
			  VALUES(SEQ_FNO.NEXTVAL, 
			  	     SEQ_BNO.CURRVAL, 
			  	     ?, 
			  	     ?, 
			  	     ?)
	</entry>
	
	
	
	
	
	
	
	<!-- 
	<entry key="searchedCsCount">
		SELECT 
				COUNT(*) 
		FROM 
				TB_CS_BOARD 
		JOIN 
				TB_MEMBER 
		USING (MEMBER_NO) 
		WHERE  
				(MEMBER_ID LIKE '%' || UPPER(?) || '%'
		    OR	
	 		 	MEMBER_ID LIKE '%' || LOWER(?) || '%')
	</entry>
	
	<entry key="searchedCsList">
	SELECT 
				*
		FROM 
				TB_CS_BOARD 
		JOIN 
				TB_MEMBER 
		USING (MEMBER_NO) 
		WHERE  
				(MEMBER_ID LIKE '%' || UPPER(?) || '%'
		    OR	
	 		 	MEMBER_ID LIKE '%' || LOWER(?) || '%')
	
	</entry>
	 -->
	
	
	
	
	
	
	
	
	
	
	
	
	
</properties>
